<% for(var i = 0; i < autores.length; i++){ %>
    <tr>
        <td><%= i+1 %></td>
        <td><%= autores[i].nome %></td>
        <td>
            <a href="/update_autor?id=<%= autores[i]._id %>" class="btn border-shadow update">
                <span class="text-gradient"><i class="fas fa-pencil-alt"></i></span>
            </a>
            <a class="btn border-shadow delete" data-id="<%= autores[i]._id %>" data-tela="autor">
                <span class="text-gradient"><i class="fas fa-trash-alt"></i></span>
            </a>
        </td>
    </tr>
<% } %>